---
- hosts: all
  become: true

  tasks:

    - name: Install apache package in any distro
      package:
        name:
          - "{{ apache_package }}"
          - "{{ php_package }}"
        state: present
        update_cache: true
     
